TODO
create quick time feature that registers success/failures
	delay every 2 seconds throw a new QTE
	fail if the next QTE would trigger
	add it so all the instances in the list will be created at once and delete them
	so when it moves to the next list recreate all the instances
	